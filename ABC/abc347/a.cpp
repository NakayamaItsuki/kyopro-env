#include <bits/stdc++.h>

using namespace std;

int main() {

    int N, H, W;
    int As[19], Bs[19];

    cin >> N >> H >> W;

    for (int i = 1; i <= N; i++) {
        cin >> As[i] >> Bs[i];
    }

    






}